@model IEnumerable<VideojuegosCRUD.Models.Desarrollador>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombreDesarrollador)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Fundacion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PaisOrigen)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SitioWeb)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreDesarrollador)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Fundacion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PaisOrigen)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SitioWeb)
            </td>
            <td>
                <a asp-action="Edit" class="btn btn-warning" asp-route-id="@item.IdDesarrollador">Edit</a> |
                <a asp-action="Details" class="btn btn-primary" asp-route-id="@item.IdDesarrollador">Details</a> |
                <a asp-action="Delete" class="btn btn-danger" asp-route-id="@item.IdDesarrollador">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
